import { FeaturePlaceholder } from "../../../app/pages/FeaturePlaceholder";

export function BookingPage() {
  return (
    <FeaturePlaceholder
      title="预约"
      stage="S0-8 迁移中"
      description="明日预约与立即抢座将在此页提供模式切换（移动端短标签：预约 / 抢座），当前预约卡与备选链在 S2 加入。"
    />
  );
}
