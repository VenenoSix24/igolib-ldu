import { FeaturePlaceholder } from "../../../app/pages/FeaturePlaceholder";

export function RenewalPage() {
  return (
    <FeaturePlaceholder
      title="续约"
      stage="S4 开发中"
      description="临近签到截止自动取消并重订（等待时长可配，默认 60 秒），安全上限内循环保座。"
    />
  );
}
